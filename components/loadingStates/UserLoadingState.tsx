import * as React from 'react';

/**
 * When building UIs that interact with an asynchronous layer (e.g. a database, a web API, a file system):
 * 
 * 1. The success state
 * 2. The error state
 * 3. The loading state
 */

interface Props {

}

const UserLoadingState: React.FC<Props> = ({ }) => {

 return (
 <div>Hi</div>
 )
}

export default UserLoadingState